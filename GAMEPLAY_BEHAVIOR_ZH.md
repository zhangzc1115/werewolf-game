# 狼人杀游戏行为规范 (Werewolf Game Behavior Specification)

本文档描述了本仓库当前版本的游戏逻辑实现行为。

## 1. 运行模型

- **服务器**: Node.js + Express + Socket.IO (`server.js`)。
- **状态存储**:
  - 运行时的内存实时状态。
  - 状态更新时持久化到 `data/game-state.json`。
  - 启动时如果文件存在则自动加载。
- **身份识别与重连**:
  - 每个玩家分配一个唯一的 Token。
  - 客户端将 Token 存储在 `localStorage` 中。
  - 刷新或重连时，服务器通过 Token 重新绑定玩家。

## 2. 角色列表

- `wolf` (狼人)
- `wolf_king` (狼枪/白狼王)
- `witch` (女巫)
- `seer` (预言家)
- `hunter` (猎人)
- `idiot` (白痴)
- `hybrid` (混血儿)
- `villager` (平民)
- **内部非玩家角色**:
  - `spectator` (旁观者)
  - `unassigned` (未分配)

## 3. 阵营划分

- **狼人阵营**:
  - `wolf` (狼人)
  - `wolf_king` (狼枪)
- **好人阵营**:
  - `villager` (平民)
  - `witch` (女巫)
  - `seer` (预言家)
  - `hunter` (猎人)
  - `idiot` (白痴)
- **混血儿**:
  - `hybrid` 的阵营取决于其选择的榜样角色。

## 4. 游戏开始与身份分配

- 上帝（管理员）通过 `admin-start` 载荷启动游戏，包含各角色配置数量。
- 至少需要 **4名** 在线玩家才能开始。
- 如果配置中 `wolf + wolfKing === 0`，服务器将强制生成至少一名 `wolf`。
- 如果配置的神职角色总数超过玩家总数，服务器将按优先级自动缩减角色。
- 剩余空位由 `villager` (平民) 填充。

## 5. 游戏阶段 (Phases)

- `waiting`: 大厅等待，游戏未开始。
- `hybrid`: 第一夜限定，混血儿选择榜样阶段。
- `wolf`: 狼人行动（狼人与狼枪共同投票击杀）。
- `witch`: 女巫行动（救人或毒人）。
- `seer`: 预言家行动（查验身份）。
- `day_announce`: 天亮结算，宣布死讯；上帝处理白天投票。
- `shoot_phase`: 共享开枪阶段，用于处理狼枪或猎人的技能触发。
- `game_over`: 达到胜利条件，游戏结束。

## 6. 夜间行动逻辑

- **行动顺序**:
  - 第一夜: `混血儿 -> 狼人 -> 女巫 -> 预言家`
  - 后续夜晚: `狼人 -> 女巫 -> 预言家`
- **混血儿 (Hybrid)**:
  - 仅第一夜，选择一名存活玩家作为榜样 (`modelId`)。
  - 不能选择自己。
  - 混血儿的胜利条件随榜样阵营变化。
- **狼人击杀 (Wolf kill)**:
  - 所有狼人阵营玩家提交击杀目标。
  - 服务器统计票数并选取票数最高的目标。
- **女巫 (Witch)**:
  - **救人**: 每晚可抵消狼人的击杀目标（仅限一次）。
  - **毒药**: 每晚可毒杀一名存活玩家（仅限一次）。
- **预言家 (Seer)**:
  - 每晚可查验一名玩家。
  - 返回结果为“好人”或“坏人”。狼人阵营（狼人、狼枪）为坏人。
  - **注意**: 混血儿无论榜样是谁，查验结果始终为“好人”。
- **夜晚结算**:
  - 应用击杀与毒药逻辑。
  - 更新 `nightResult.deadIds` (死亡名单)。
  - 记录死亡原因（狼刀、毒杀、狙击）以触发后续技能。

## 7. 白天投票、白痴及开枪触发

- 上帝使用 `admin-day-vote` 标记投票出局目标。
- **如果是白痴 (`idiot`) 被投且未翻牌**:
  - 玩家免疫此次死亡。
  - `isExposed` (已翻牌) 设置为 true。
  - `canVote` (投票权) 设置为 false。
  - 广播公共公告，不进入开枪阶段。
- **如果是狼枪 (`wolf_king`) 被投出**:
  - 进入 `shoot_phase` (开枪阶段)。
- **如果是猎人 (`hunter`) 被投出**:
  - 进入 `shoot_phase` (开枪阶段)。
- **其他角色**:
  - 正常出局，检查胜利条件。

## 8. 开枪阶段逻辑 (Shoot Phase)

- **操作**:
  - 开枪者发送 `action: { type: 'shoot', targetId }`。
  - 目标立即被淘汰。
- **触发源**:
  - 白天票出狼枪或猎人。
  - 猎人死于夜晚狼刀（且未被女巫毒杀）。
- **限制**:
  - 如果猎人被 **毒杀**，则无法开枪。
  - 狼枪在夜间死亡时无法触发开枪。

## 9. 胜利条件

- **有效狼人计数**: 存活狼人 + 存活狼枪 + 榜样为狼人的存活混血儿。
- **有效好人计数**: 存活神职及平民 + 榜样为好人的存活混血儿。
- **好人胜利**: 有效狼人计数为 `0`。
- **狼人胜利**: 有效好人计数为 `0`。
- **平局**: 双方计数同时为 `0`。
- **结算**: 标记 `phase = game_over`，计算并广播 `winner` 信息。

## 10. 可见性规则

- **上帝 (Admin)**: 可见所有人的身份。
- **玩家 (Player)**:
  - 可见自己的身份。
  - 混血儿可见自己的榜样。
  - 可见所有人的存活/连接状态，以及特定标记（如无票、已翻牌）。
  - 除非白痴翻牌，否则其他人的身份保持隐藏。

## 11. 上帝控制功能

- 启动游戏（配置各角色人数）。
- 手动推进阶段（混血儿 -> 狼人 -> 女巫 -> 预言家 -> 天亮）。
- 白天投票结算（手动输入出局玩家 ID）。

## 12. 已知限制

- **投票系统**: 暂无自动化的白天投票系统，需上帝手动输入结果。
- **房间限制**: 单房间设计，不支持多局游戏并行。
- **流程推进**: 游戏高度依赖上帝手动点击“下一阶段”进行驱动。
